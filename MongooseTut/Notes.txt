To install Mongoose in this folder:
npm install mongoose

Lecture WDC 86: How to store data into DataBase
Connecting from using methods database should be same.

You Always need to specify connection string.
You Always need to run mongod server.
MongoDB Compass works as the GUI to interact with MongoDB, It is not itself the server.

// Start using mongoose

We need to create the relation schema first:
Which defines the field, datatypes, datawhich is going to insert in the table and also restrictions all of this make the structure of the relation.

Then we can insert, create the documents objects in the relation.

// While actual work we don't use MongoShell to write the queries.
// We actually use and write this queries in the mongoose code.