console.log("This is tutorial WDC_69.js");


```javascript
// This is tutorial for npm commands, use & version.

// Major.minor.patch
// 1.0.0 main software version.
// 1.0.1 after fixing problems & bugs.
// 1.1.0 after adding some functions & features.
// 2.0.0 after get major upadate & remove some functions.

// It Works for package.json file
// npm commands to install
  // npm --version to check version
  // npm i name - to install anything
  // npm install also create package.lock.json with node_modules folder: contain all folder related packages dependencies and modules

// npm install slugify
// npm install browserify
// npm install nodemon
// npm install nodemom --global - to install globally

// after deleting node modules 'npm i': reinstall all
// node modules contain all dependencies with its dependencies

// To view packages version
// npm view [packageName] version
```
